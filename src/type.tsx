export interface MOVIE {
    id: number;
    name: string;
    year: string;
    category: string;
    description: string;
    pic: string;
}

export interface CATEGORY {
    id: number,
    category: string;
}